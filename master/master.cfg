from master.clients import MasterClient

client = MasterClient.try_token_path("/tokens/master/token.json")
repo = "https://github.com/JoelLefkowitz/pub"

BuildmasterConfig = {
    "title": "Pipeline",
    "buildbotURL": client.buildbot_url,
    "workers": client.workers,
    "protocols": client.pb,
    "www": client.www,
    "db": client.postgres("postgres"),
    "builders": client.checkout_builder("tests", [("pytest")]),
    "change_source": client.change_source(repo),
    "schedulers": client.scheduler("pre-publish", repo, ["tests"]),
}
