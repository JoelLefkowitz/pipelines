import os
import sys

from architects import (buildbotURL, builders, changes, pb, pool, schedulers,
                        secrets, sqlite, www)

host = "localhost"
workers = pool(4)

BuildmasterConfig = {
    "title": "Pipeline",
    "buildbotURL": buildbotURL(host, 8010),
    "workers": workers,
    "protocols": pb(9989),
    "www": www(8010),
    "db": sqlite,
    "change_source": changes,
    "schedulers": schedulers,
    "builders": builders(workers),
    "secretsProviders": secrets,
}
